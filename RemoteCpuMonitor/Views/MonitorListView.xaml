<UserControl x:Class="RemoteCpuMonitor.Views.MonitorListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RemoteCpuMonitor.ViewModels"
             xmlns:views="clr-namespace:RemoteCpuMonitor.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             
             >
    <Grid>
        <StackPanel HorizontalAlignment="Stretch">
            <ListView Margin="4" ItemsSource="{Binding HostList}" HorizontalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                        <StackPanel>
                            <TextBox Text="{Binding Hostname}" />
                            <views:MyListControlView HorizontalAlignment="Stretch" />
                        </StackPanel>
                    </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
            
        </StackPanel>
    </Grid>
</UserControl>
